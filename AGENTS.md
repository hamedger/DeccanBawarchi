# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Launch target
**Web only** — ship via `expo export` + Firebase Hosting (`dist/`). Do not add iOS/Android launch work, native-only features, or App Store / Play Store config unless explicitly requested. Prefer web APIs and `Platform.OS === 'web'` checks where native behavior differs.
